import type { Metadata } from "next";
export const metadata: Metadata={title:"Data Quality Triage — Midnight Command",description:"Deterministic warehouse health and incident lifecycle operations."};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
